apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: vault-role
  namespace: vault
rules:
- apiGroups: [""]
  resources:
    - pods
    - secrets
    - services
    - configmaps
  verbs:
    - get
    - list
    - watch